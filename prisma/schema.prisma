// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
    url      = "postgres://vujjgkqnwqjzky:97b333f80e000f91563a3f6b455265d5f284957b0c6827c3aec7085e4f21fce3@ec2-44-195-169-163.compute-1.amazonaws.com:5432/d7ideukirqcpju"
}

model User {
    id        Int        @id @default(autoincrement())
    email     String     @unique
    name      String
    password  String
    createdAt DateTime   @default(now())
    updatedAt DateTime   @updatedAt
    languages Language[]
    words     Word[]
}

model Language {
    id        Int    @id @default(autoincrement())
    name      String @unique
    shortName String @unique
    users     User[]
    words     Word[]
}

model Word {
    id         Int      @id @default(autoincrement())
    userId     Int
    languageId Int
    user       User     @relation(fields: [userId], references: [id])
    language   Language @relation(fields: [languageId], references: [id])
}
